import { State } from '@/store/interface'
// - 类型 约束 报错
// - 代码 友好
const getters = {}
const state: State = {
  movieList: []
}
const mutations = {}
const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}